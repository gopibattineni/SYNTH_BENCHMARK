# SYNTH Benchmark — Python dependencies
# Tested with Python 3.10–3.12 on Windows.
#
# Install:
#   python -m venv .venv
#   .venv\Scripts\activate          # Windows
#   source .venv/bin/activate       # Linux / macOS
#   pip install --upgrade pip
#   pip install -r requirements.txt
#   pip install "libzero==0.0.8" "rtdl==0.0.13" --no-deps
#
# PyTorch: this file installs the default (CPU) wheel. For CUDA, install torch
# from https://pytorch.org first, then run pip install -r requirements.txt.
#
# Not installable via pip (clone manually — see README):
#   _vendor/tab-ddpm          TabDDPM diffusion generator
#   Other GANS/CTAB-GAN-Plus/ CTAB-GAN+ notebooks

# --- Jupyter (run .ipynb notebooks) ---
ipykernel>=6.29
jupyterlab>=4.0
notebook>=7.0

# --- Core data science ---
numpy>=1.24,<3
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.13

# --- Excel I/O (TRTR/TSTR exports, audit workbooks) ---
openpyxl>=3.1
xlsxwriter>=3.1

# --- Synthetic data generators (SDV) ---
sdv>=1.17

# --- Deep learning ---
torch>=2.0

# --- Dataset loaders & metrics ---
ucimlrepo>=0.0.7
gower>=0.1.2

# --- Diffusion generators (ForestDiffusion + TabDDPM helpers) ---
ForestDiffusion>=1.0
xgboost>=2.0
category-encoders>=2.6
imbalanced-learn>=0.12
absl-py>=2.0
tensorboardX>=2.6
icecream>=2.1
dython>=0.7
optuna>=3.0
skorch>=1.0
pyarrow>=14.0
tomli>=2.0
tomli-w>=1.0
pynvml>=11,<12

# libzero and rtdl pin torch<2 in their metadata but work with torch 2.x.
# Install AFTER the lines above (see header comment):
#   pip install "libzero==0.0.8" "rtdl==0.0.13" --no-deps
